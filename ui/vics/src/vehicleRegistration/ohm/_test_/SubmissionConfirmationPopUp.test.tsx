
import "@testing-library/jest-dom";


 describe("SubmissionConfirmationPopUp", () => {
   test("renders correctly", () => {
//     const props = {
//       showSubmissionConfirmationPopUp: "true",
//       setSubmissionConfirmationPopUp: jest.fn(),
//     };

//     render(<SubmissionConfirmationPopUp {...props} />);

//     expect(screen.getByText("Submission Confirmation")).toBeInTheDocument();
//     expect(
//       screen.getByText("Are you sure to submit inspection results?")
//     ).toBeInTheDocument();
//     expect(screen.getByText("Yes")).toBeInTheDocument();
//     expect(screen.getByText("No")).toBeInTheDocument();
  });

//   test("calls setSubmissionConfirmationPopUp with false when 'No' button is clicked", () => {
//     const setSubmissionConfirmationPopUp = jest.fn();
//     const props = {
//       showSubmissionConfirmationPopUp: "true",
//       setSubmissionConfirmationPopUp,
//     };

//     render(<SubmissionConfirmationPopUp {...props} />);

//     const noButton = screen.getByText("No");
//     fireEvent.click(noButton);

//     expect(setSubmissionConfirmationPopUp).toHaveBeenCalledWith(false);
//   });
 });
