export interface SubmissionConfirmationState {
  brake?: {
    isAccepted?: boolean;
  };
  ohm?: {
    isAccepted?: boolean;
  };
  ht?: {
    isAccepted?: boolean;
  };
  exhaust?: {
    isAccepted?: boolean;
  };
  vi?: {
    isAccepted?: boolean;
  };
  speedometer?: {
    isAccepted?: boolean;
  };
  uci?: {
    isAccepted?: boolean;
  };
  inspection?: {
    isAccepted?: boolean;
  };
}
